# Telegram Bot Integration для отправки заявок

Этот проект интегрирован с Telegram Bot API для отправки заявок с контактной формы в Telegram.

## Файлы интеграции

- `send_to_telegram.php` - PHP-скрипт для отправки заявок в Telegram
- Модифицированные компоненты формы:
  - `src/components/Contact.tsx` - английская версия
  - `src/components/ru/Contact-ru.tsx` - русская версия

## Настройка Telegram Bot

### 1. Создание бота

1. Найдите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Выберите имя для бота (например, "INTEGRAM Applications")
4. Выберите username для бота (например, "integram_applications_bot")
5. Сохраните токен бота (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получение Chat ID

1. Добавьте бота в вашу группу или начните чат с ботом
2. Отправьте любое сообщение боту
3. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите `chat.id` в ответе (это ваш Chat ID)

### 3. Конфигурация

Откройте файл `send_to_telegram.php` и замените:

```php
$bot_token = 'YOUR_BOT_TOKEN_HERE'; // Замените на ваш токен бота
$chat_id = 'YOUR_CHAT_ID_HERE';     // Замените на ваш Chat ID
```

## Формат сообщений

Заявки отправляются в следующем формате:

```
🆕 Новая заявка с сайта INTEGRAM

👤 Имя: [Имя пользователя]
📞 Телефон: [Телефон]
📧 Email: [Email]
🏢 Компания: [Компания] (опционально)
💬 Сообщение:
[Текст сообщения]

🕐 Время: [Дата и время]
🌐 Источник: [Домен]
```

## Развертывание

### Локальная разработка

1. Установите PHP (рекомендуется PHP 7.4+)
2. Разместите проект в папке веб-сервера
3. Обновите токен и Chat ID в `send_to_telegram.php`
4. Откройте сайт в браузере

### Продакшн

1. Загрузите файлы на сервер
2. Убедитесь, что PHP включен
3. Настройте HTTPS (рекомендуется)
4. Обновите токен и Chat ID в `send_to_telegram.php`

## Безопасность

- Никогда не коммитьте токен бота в публичный репозиторий
- Используйте переменные окружения для хранения секретов
- В продакшне ограничьте доступ к PHP-скрипту только с вашего домена

## Логирование

PHP-скрипт возвращает JSON-ответы:
- Успех: `{"success": true, "message": "Заявка успешно отправлена!"}`
- Ошибка: `{"error": "Описание ошибки"}`

## Тестирование

Для тестирования откройте DevTools в браузере и проверьте:
1. Network tab - запросы к `/send_to_telegram.php`
2. Console - ошибки JavaScript
3. Toast уведомления о статусе отправки

## Поддержка

При возникновении проблем:
1. Проверьте логи PHP на сервере
2. Убедитесь, что cURL доступен в PHP
3. Проверьте токен и Chat ID
4. Проверьте сетевые запросы в DevTools